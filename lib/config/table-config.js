/*
  Configuraions available for table component in Semantic UI
*/
// celled: true,  //whether to have border for each table column/cell
// striped: true,  //alternate row with white & ligh grey variant color
// selectable: true, //highligthing the color or the row on hover
// unstackable: true //specify how it stacks table content responsively
// fixed: true,  //takes equal width for each column, does not change based on column content
// singleLine:true,  //if the text is too lengthy, it will show ellipses...
// size: "small", "large",  //whether to have more padding & font size
// collapsing:true  //column width will be adjusted based on content lenght (do not take full widht of the screen)
// textAlign="center" "left", "right"  // alignment of the text

export const tableDefaults = {
  celled:true,
  striped: true,
  selectable: true,
  unstackable: true,
  fixed: true,
  compact: true,
  singleLine:true,
  size: "small",
  collapsing: true
}
